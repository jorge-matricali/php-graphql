namespace GraphQL\Executor;

class ExecutorexecuteFieldsSeriallyClosureSeven
{

    public function __construct()
    {
        
    }

    public function __invoke(resolvedResults)
    {
    return self::fixResultsIfEmptyArray(resolvedResults);
    }
}
    