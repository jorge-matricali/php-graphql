namespace GraphQL\Utils;

class SchemaPrinterprintFilteredSchemaClosureFour
{
    private options;

    public function __construct(options)
    {
                let this->options = options;

    }

    public function __invoke(type)
    {
    return self::printType(type, this->options);
    }
}
    