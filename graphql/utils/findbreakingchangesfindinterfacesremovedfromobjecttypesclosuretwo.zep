namespace GraphQL\Utils;

class FindBreakingChangesfindInterfacesRemovedFromObjectTypesClosureTwo
{
    private oldInterface;

    public function __construct(oldInterface)
    {
                let this->oldInterface = oldInterface;

    }

    public function __invoke(InterfaceType interface)
    {
    return interfacee->name === this->oldInterface->name;
    }
}
    