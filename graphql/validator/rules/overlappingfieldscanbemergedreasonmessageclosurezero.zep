namespace GraphQL\Validator\Rules;

class OverlappingFieldsCanBeMergedreasonMessageClosureZero
{

    public function __construct()
    {
        
    }

    public function __invoke(tmp)
    {
    tmpListResponseNameSubReason;
    reasonMessage;
    return "subfields \"{responseName}\" conflict because {reasonMessage}";
    }
}
    