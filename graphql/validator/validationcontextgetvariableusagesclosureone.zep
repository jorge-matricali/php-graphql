namespace GraphQL\Validator;

class ValidationContextgetVariableUsagesClosureOne
{
    private newUsages;
    private typeInfo;

    public function __construct(newUsages, typeInfo)
    {
                let this->newUsages = newUsages;
        let this->typeInfo = typeInfo;

    }

    public function __invoke(VariableNode variable)
    {
    this->newUsages[];
    }
}
    